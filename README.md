## CSS-only expansion of a table

Short experiment to figure out how to toggle table rows using only CSS.

No idea how accessible e.g. screenreader friendly this is.

Serve and watch the html with `make serve`.